#include "Cube.h"
#include "Render/Renderer.h"

void Cube::OnInit()
{
    Renderer::OnInit();
}

void Cube::OnUpdate()
{
    // rotation.y += deltaTime * 10;
    // renderer->rotation.x += deltaTime * 10;
}